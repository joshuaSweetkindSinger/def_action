<li>
  <%= link_to(user_profile_path(user)) do %>
    <%= gravatar_for user, size: 52 %>
    <%= user.name %>
    <%= user.email %>
  <% end %>
  <% if user.admin? %>
    is an admin
  <% end %>
  <% if current_user.admin? && !current_user?(user) %>
    |  <%= link_to('delete', delete_user_path(user), method: :delete, confirm: 'You sure?') %>
  <% end %>
</li>