<!-- If the user was properly authenticated, take them to their desired page,
or, to the root page if there is no cached page waiting ot be shown. If the
user was not properly authenticated, take them back to the sign in page. -->

<%
   if @authenticated
     redirect_back_or root_path
   else
     flash[:error] = 'Invalid email/password combination'
     redirect_to 'sign_in_page'
   end
%>


