<%= provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate @users %>

<ul class = "users">
  <%= render partial: 'shared/user', collection: @users %>
</ul>

<%= will_paginate @users %>

