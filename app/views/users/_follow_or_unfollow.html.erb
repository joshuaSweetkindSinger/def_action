<% unless false # current_user?(@user) %>
  <div id = "follow_or_unfollow_button">
    <% if current_user.following?(@user) %>
      <%= render 'unfollow_button' %>
    <% else %>
      <%= render 'follow_button' %>
    <% end %>
  </div>
<% end %>